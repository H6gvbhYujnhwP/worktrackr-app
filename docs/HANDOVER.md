# WorkTrackr — Handover (read me first)

**Last updated: end of the post-Manus integration + feature session.**
**Current build stamp: `2026-08-05.editable-notes`** (shown faintly in the sidebar footer when the sidebar is expanded — use it to confirm a deploy went live).

This file is the quick orientation for anyone (or any AI assistant) picking up WorkTrackr. The full detail is in the roadmap; this is the map.

---

## 🚦 WHERE THINGS STAND NOW — READ THIS FIRST
The big visual redesign is **DONE and live**. **Manus AI's dark "Relationship Hub" redesign (two rounds) has been integrated and verified, and every remaining light screen has been darkened** — the whole app is now dark, full-width, end-to-end. We are **no longer reskinning**; the work now is **feature-building, fixing flows, and polish**, one focused change per turn.

**What this means for a fresh chat:**
- The old "page-by-page dark reskin" plan and the "don't flip the whole app dark / wait for Manus" instructions in earlier versions of these docs are **superseded and no longer apply.** `App.css` is now globally dark.
- The redesigned frontend is **Manus's** (we verified Manus's round-2 full-repo return left the **backend byte-for-byte identical** and took only the frontend + kept the owner's `docs/`).
- Backend was **untouched** the entire redesign — until this session's **one** backend change (company file attachments, see below). Backend changes are now allowed but should stay **rare, minimal, and explicitly flagged** to the owner.

---

## 🗓️ SHARED DATE-PICKER ROLLOUT — Phase 1 DONE, Phase 2 plain-pickers DONE, **tickets / H&S / date-time remain**

The owner wants the fiddly native `<input type="date">` boxes replaced with a **pop-up calendar** across the app, done **in phases**. Phase 1 and the Phase-2 plain day-pickers have shipped; **what remains is the ticket screens, Health & Safety, and the date+time fields.**

**The shared component:** `web/client/src/app/src/components/DatePicker.jsx` (NEW in Phase 1).
- Props: `value` = `'YYYY-MM-DD'` string · `onChange(v)` = receives the **string directly (NOT an event)** · `className` = pass the form's existing input classes so the trigger looks identical · optional `style` = pass the form's inline `style` object for screens that style inputs inline instead of with classes (added Phase 2, used by `CompanyProfile.jsx`; layout hints like `width`/`flex`/`alignSelf`/`minWidth` are lifted onto the wrapper so the trigger sizes right in a flex row) · optional `placeholder`.
- **No minimum date on purpose — past dates ARE allowed** (holiday and other past-dated requests must go through the normal approval flow). Do **not** add a `min`.
- The pop-up is always dark-themed; the trigger button inherits `className` and/or `style`.

**Apply pattern (per field):**
1. `import DatePicker from './DatePicker.jsx';`
2. Swap `<input type="date" value={X} onChange={(e)=>setX(e.target.value)} className={CLS} />`
   → `<DatePicker value={X} onChange={setX} className={CLS} />`.
   If it sets an object field: `onChange={(v)=>setForm({ ...form, field: v })}`.
3. Keep the **same `className`** the input used.

**✅ Phase 1 (DONE — stamp `2026-06-27.datepicker-phase1`):**
`MyHoliday.jsx` (request From/To), `HolidayManager.jsx` (Holiday admin → Settings company-year From/To — the field the owner flagged), `MyTasks.jsx` (due date), `PersonalNotes.jsx` (note due date), `EngineerWageAdmin.jsx` (started-at), `InvoicesList.jsx` (issue + due), `QuoteForm.jsx` (valid-until).

**✅ Phase 2 — plain day-pickers (DONE — stamp `2026-07-01.datepicker-phase2`):**
- `AddLeadModal.jsx` — firstContact, chaseDate (className swap).
- `CompanyProfile.jsx` — chaseDate, noteCalDate, reminderForm.date, taskForm.dueDate. These used inline `style={inputStyle}`, so DatePicker was extended with a `style` prop (see above); the **date** halves were swapped and the neighbouring native **time** boxes were left as-is (those become the date+time batch below).
- `CRMDashboard.jsx` — per-service Renewal date (className swap).
- `InvoiceDetail.jsx` — Due date (className swap; keeps `setDirty(true)`).

**✅ Phase 2 — COMPLETE. Every reachable date/time field now uses the shared pickers.**
- Plain day-pickers: Phase-1 seven screens + Add Lead, Company profile, CRM dashboard, Invoice detail, ticket detail screens, and all 11 Health & Safety fields.
- Date + time: `DateTimePicker` (day + 15-min time) and `TimePicker` (15-min) across CRMCalendar, CreateTicketModal, JobForm, VoiceAssistant, CompanyProfile times, XeroIntegration. Minutes are **00/15/30/45** only.
- Components: `DatePicker.jsx` (day, `value`/`onChange` = `'YYYY-MM-DD'`, accepts `className` + `style`), `DateTimePicker.jsx` (default `DateTimePicker` = `'YYYY-MM-DDTHH:MM'`; named `TimePicker` = `'HH:MM'`).
- The only native date/time inputs left are inside **dead/unrendered files** — if any of those is ever switched on, swap its fields to these components (and constrain any native `type="time"` to `step="900"`).

*Date **+ time** fields (DatePicker is day-only — need a time-aware `DateTimePicker` variant, or leave):*
- `CRMCalendar.jsx` (~927 datetime-local, ~1185, ~1251, ~1361), `IntegratedCalendar.jsx` (~497), `BookingCalendar.jsx` (~535), `JobForm.jsx` (~301, ~310 datetime-local), `XeroIntegration.jsx` (~268), `VoiceAssistant.jsx` (~409, ~525, ~534, ~570).

Ignore the dead `*_broken.jsx` files (`CreateTicketModal_broken`, `TicketFieldCustomizer_broken`).
⚠️ Modal caveat: the pop-up is absolutely-positioned; inside a modal with `overflow-hidden`, a picker near the bottom edge could clip. None seen so far in Phase 1; if it happens in Phase 2, render the pop-up via a portal.

---

## Work shipped THIS session (post-Manus, feature/fix session)
All frontend unless flagged **backend**. Each shipped as changed-files only and bumped the stamp. Newest first:
- **Editable notes + "Add reminder" from a note, and "No stage" selectable again** (⚠️ **BACKEND + NEW MIGRATION** — `routes/contacts.js`, new `migrations/phase12_contact_notes_updated.sql`; plus `CompanyProfile.jsx`). **(A) "No stage" bug — was TWO faults, not one.** The dropdown had `<option value="" disabled>Set stage…</option>`, so once a company had any stage it could never go back (Source, immediately beside it, has always used a plain selectable `—`). But un-greying alone would have produced a **worse** bug: `salesStage` was `z.enum([...]).optional()`, and **`.optional()` permits absent, NOT null** — so the blank choice would have looked live and then failed validation on save. Fixed both ends: option now reads **"No stage"** and is selectable, `setStage` sends `stage || null`, and the schema is now `.optional().nullable()`. `valuesFor()` in `CompanyPipelineList` already buckets a missing stage as `NO_STAGE`, so the badge/filter side needed nothing. **(B) Editable notes.** There was only ONE note endpoint on the server (create) — hence no edit button to find anywhere. Added **`PUT /api/contacts/:id/notes/:noteId`**. Owner chose **anyone in the organisation may edit any note** (sales log is shared; a colleague fixing a typo shouldn't need a manager) — so, deliberately, **no creator/manager gate here**, unlike `saved-searches.js`. Still scoped by **both `organisation_id` AND `contact_id`**, so a note can only be reached through the company it belongs to; empty bodies rejected **before** the UPDATE; `COALESCE($2, subject)` preserves the subject on logged emails when only the body is sent; missing row → 404. **New migration adds `updated_at` + `updated_by`** — without them an edited note would keep claiming its original timestamp. History now selects `n.updated_at` and joins a second `users` alias for the editor's name, returning `editedAt`/`editor`, and the timeline shows "· edited by X". ⚠️ **MIGRATION ORDERING HAZARD (important for future migrations):** `run-migrations.js` sorts filenames **alphabetically, not numerically**, so the real order is `…phase10_, phase11_, phase12_, phase4_ … phase8_, phase9_` — meaning **`phase12_` runs BEFORE `phase8_contact_notes.sql`, the file that creates the table**. On the live DB the table already exists so a bare `ALTER` would have worked, but on a **fresh** database it would have failed on boot. `phase12` is therefore deliberately **self-sufficient and order-independent**: `CREATE TABLE IF NOT EXISTS contact_notes` (definition identical to phase8, which then no-ops) followed by `ADD COLUMN IF NOT EXISTS`. A test asserts the sort hazard is real so the safeguard isn't mistaken for cargo-culting. **(C) Add reminder from a note.** Timeline rows are prefixed by source (`n_` note, `e_` crm_event, `t_` task); only `n_` rows get **Edit** (in-place textarea, Save/Cancel, disabled while empty or saving) and **Add reminder**, which opens the existing reminder box pre-filled with the note's text as both title (first 80 chars) and detail, **date left blank on purpose** so the user must choose one — which the previous change made impossible to skip silently. Calendar entries keep their "View in calendar" link; tasks get neither. **Verified:** `npm run build` passes, **1852 modules**, zero errors; `node --check` clean on `contacts.js`; migration parsed with sqlglot as Postgres (3 statements); **46 new tests** covering both halves of the No-stage fix (incl. that the server would otherwise have rejected the blank option, and that all seven stages remain valid), the endpoint's org+contact scoping, empty-body rejection ordering, edit-stamp recording, 404 path, the deliberate absence of an author gate, the `n_`-only gating of Edit/Add reminder, and the migration's order-independence; **252/252 prior tests still passing — 298 total, no regression**. `editable-notes`.
- **Calendar reminders: not reaching the calendar, no company/notes on the event, no link from the timeline** (frontend only — **NO backend change**; `CompanyProfile.jsx`, `CRMCalendar.jsx`, `Dashboard.jsx`). Owner reported three things after adding "a note with a calendar reminder" on Rosetti. **Traced the whole path before touching anything.** `/api/crm-events` exists, is mounted with auth, its POST stores every field, and its GET returns `c.name as contact_name` via a join — **the backend was never the problem**. **(1) The reminder was never created.** Evidence from the owner's own screenshot: `GET /api/contacts/:id/history` **does** include `crm_events` rows (kind = the event `type`, sorted by `at` DESC, top 50), so a created reminder always appears in the company timeline as "Follow-up — …", and a future-dated one sorts to the **top**. The screenshot's timeline showed only the note ⇒ no event row existed. Cause: owner is still on **`dead-stage`** (visible in their sidebar) — the `note-feedback` build with the silent-guard fix **was not yet deployed**, and `submitReminder`'s old bare `if (!title || !date) return;` did nothing visible with an empty title. **A SECOND silent trap was still live and is fixed here:** `saveNote`'s `if (noteToCal && noteCalDate)` meant ticking "Add to calendar" **without** picking a date saved the note and skipped the calendar entry **without a word** — you'd believe a reminder existed when none did. Now checked **before** the note POST (so nothing is half-done) with a hint naming both ways out ("Pick a date… or untick"). Both creation paths now also `window.dispatchEvent(new Event('worktrackr:crm-event-created'))` — CRMCalendar already listens for that (it was only ever fired by VoiceAssistant), so an open calendar refreshes itself. Also worth knowing: a September reminder legitimately won't appear while viewing August. **(2) Company + notes on the event.** The detail panel **already rendered** `selectedEvent.company` and `selectedEvent.notes` — both were always blank for two separate reasons: the loader never copied the API's `contact_name` into the `company` field the panel reads (**the data arrived and was dropped**), and reminders were created with no description at all. Fixed by mapping `company: ev.company || ev.contact_name || ''` and `notes: ev.notes || ev.description || ''` in `loadEventsFromAPI` (guarded with `Array.isArray(rows) ? rows : []`), by sending `description`/`notes` on both creation paths (note text for the checkbox path; a **new optional "What's it about?" textarea** added to the reminder panel for the panel path — previously there was nowhere to put context). **(3) Timeline → calendar link.** Timeline entries whose id starts with **`e_`** (the history endpoint's prefix for `crm_events`; `n_` = note, `t_` = task) now render a **"View in calendar"** button. New `onOpenCalendar` prop on `CompanyProfile`, wired in `Dashboard.jsx` following the existing `ordersInitial` pattern (`setCalendarInitial(dayStr); onViewChange('sales-calendar')`), and a new `initialDate` prop on `CRMCalendar` that seeds `selectedDate` — so a September follow-up **opens September, not today**, which is precisely what made created reminders look missing. `key={calendarInitial || 'today'}` forces a remount so a second click on a different date re-seeds. ⚠️ **Date trap handled:** the link's day string uses a new `ymdLocal()` built from local getters, **not `toISOString()`**, which would shift a local-midnight event back a day under British Summer Time and open the wrong date — the same class of bug as the MyTasks "Invalid Date" and the chase-date ISO handling. A unit test asserts local midnight on 16 Sep 2026 stays 16 Sep under `TZ=Europe/London`. **Verified:** `npm run build` passes, **1852 modules**, zero errors; `node --check` clean on `crm-events.js`; **39 new tests** run under `TZ=Europe/London` covering the closed trap (incl. asserting the check precedes the note POST so nothing is half-saved), the refresh event, both description/notes payloads, the contact_name→company mapping and non-array guard, the full prop chain profile→dashboard→calendar, and the date helper — **which caught a real edge case during development: `new Date(null)` is the 1970 epoch, not invalid, so `ymdLocal(null)` returned "1970-01-01" and would have opened the calendar in 1970; a falsy guard was added.** **213/213 prior tests still passing — 252 total, no regression.** `reminder-calendar-link`.
- **"Save note" and "Add to calendar" appeared to do nothing** (frontend only — **NO backend change**; changed `CompanyProfile.jsx` only). Owner reported both buttons dead on a company; console was clean (only the unrelated manifest-icon warning and a browser-extension listener error). **Nothing was broken.** From the screenshot: the real note box still showed its placeholder (so it was **empty**), and the owner's text was sitting in the **"Optional note to attach with the file(s)…"** field further down — which belongs to the FILE uploader and is only read at upload time (`if (attachNote.trim()) fd.append('note', …)`, line ~325). Meanwhile the reminder panel's title box was empty. Both handlers then hit **silent bare guards** — `saveNote`: `if (!body) return;` and `submitReminder`: `if (!title || !date) return;` — no message, no feedback. Compounding it, Save note was `disabled` when empty yet styled `cursor:'pointer'` at `opacity 0.6`, so a genuinely disabled control **looked perfectly clickable**. Owner chose to fix all three. **(1) Buttons now explain instead of stopping:** `saveNote` sets an inline hint ("Type your note in the box above first…") and **focuses the note textarea** via a new `noteRef`; `submitReminder` names the *specific* missing field (missing title → focuses it via `reminderTitleRef`; missing date → says so) rather than one generic message. Hints clear the moment the user types, and on Cancel. **(2) Buttons now look honest:** Save note is **no longer `disabled` merely for being empty** — it stays clickable *so that it can explain itself*, which is strictly better than a dead disabled control, and is dimmed to `opacity 0.5` when there's nothing to save; `disabled` is now reserved for the in-flight save, which also shows "Saving…". The reminder's Add to calendar dims until it has both a title and a date. **(3) The misleading field is fixed at source:** the attachment note input was a bare text box sitting loose a couple of inches under the note area with nothing tying it to the uploader. It's now **inside a bordered, uppercase-labelled "ATTACHMENTS" block** together with the drop zone, its placeholder changed from "Optional note to attach with the file(s)…" to **"Optional label for the file(s) above…"**, with helper text underneath: *"Only used when you attach a file. To log a call or note, use the box at the top."* Behaviour unchanged — it still posts as the file's note on upload. **Verified:** `npm run build` passes, **1852 modules**, zero errors; **31 new tests that parse the REAL source** asserting both bare `return`s are gone while the guards still refuse invalid input (i.e. it explains *without* starting to post), the hints are set/rendered/cleared, the focus calls target the right refs, the dimming and disabled expressions are exactly right, the old misleading placeholder is gone and the new steer is present — **plus assertions that all three endpoints are still wired** (`/api/contacts/:id/notes`, `/api/crm-events`, and the attachment note on upload) so the cosmetic work didn't quietly unhook anything; **182/182 prior tests still passing — 213 total, no regression**. `note-feedback`.
- **New "Dead" sales stage** (⚠️ **BACKEND** — `routes/contacts.js`; plus 4 frontend files). Owner asked for a Dead stage in the company Stage dropdown and a matching badge on the Companies list. **Stage keys live in SIX separate places** and all six had to move together or the stage silently half-works (saves but doesn't show, or shows but won't save): `routes/contacts.js` **line 30 zod enum** (what can be SAVED — a new value here is exactly the "add it to the schema or it vanishes on save" guardrail) and **line 332 `VALID_STAGES`** (what the **CSV import** will accept); then `CompanyPipelineList.jsx` `STAGES` (badges), `CompanyProfile.jsx` `STAGES` (the dropdown), `AddCompanyPage.jsx` `STAGES`, and `CsvImport.jsx` `STAGE_OPTIONS`. Key is **`dead`**, label **"Dead"**, placed **last (after Customer)** as a terminal state, with its own rose pill `bg-[rgba(239,68,68,0.20)] text-[#fca5a5]` / dot `#ef4444` — distinct from Suspect's grey and Customer's green. **Checked before building:** nothing in the app computes funnel or conversion metrics by iterating the stage list (only `ConvertToCustomerModal.jsx` and the "Mark won" button reference a specific stage, both hardcoded to `'customer'`), so adding a stage **cannot skew any numbers**. Pipeline view uses a wrapping responsive grid (`grid-cols-1 md:grid-cols-2 xl:grid-cols-4`), so a 7th column simply flows to the next row — **no layout change needed**. **`LeadsList.jsx` deliberately NOT touched:** its `LEAD_KEYS` is `new|prospect|hot_prospect`, so a company moved to Dead **drops off the Leads list by itself**, which is the desired behaviour; `AddLeadModal.jsx` likewise left alone since Dead isn't a lead-creation stage. Dead is a normal stage in every other respect — it gets a filter badge, is tick-boxable in the Filter pop-up, and is included in the "All" count. **Verified:** `npm run build` passes, **1852 modules**, zero errors; `node --check` clean on `contacts.js`; **16 new consistency tests that parse the REAL files** and assert all six stage definitions list exactly the same seven keys in the same order (this guards against future drift, which is the failure mode this change is most exposed to), that Dead is both saveable and importable, that its badge colours are unique across all seven, and that the Leads screen still excludes it; **plus a direct check that the filter treats `dead` as a real stage rather than bucketing it into "No stage"**, and **166/166 prior tests still passing — 182 total, no regression**. `dead-stage`.
- **Shared, named SAVED SEARCHES on the Companies filter** — ⚠️ **BACKEND + NEW DB TABLE + NEW MIGRATION + server.js change.** Biggest change in a while; 6 files, 2 of them new. Owner's three choices: **named** (press Save, give it a name — not an automatic history), picking one **runs it immediately and closes the pop-up**, and it is **shared with the whole team** (server-stored, not browser-stored), shown in a **drop-down menu**. Permissions as agreed: **anyone in the organisation may save; only the creator or a manager/admin/owner may delete**. **NEW FILES:** `web/migrations/phase11_saved_searches.sql` and `web/routes/saved-searches.js`. **`server.js` changed** (2 lines: require + `app.use('/api/saved-searches', authenticateToken, savedSearchesRoutes)` mounted beside sales-permissions). Migrations **auto-run on boot** via `run-migrations.js` (alphabetical, tracked in `schema_migrations`, skipped once applied) — `phase11_` sorts after `phase10_`, so **no manual DB step for the owner**. **Table `saved_searches`:** org-scoped FK with `ON DELETE CASCADE`, `scope` (defaults `'companies'`, so the same table can serve other lists later with no further migration), `name`, `filters JSONB`, `summary TEXT`, `created_by`, timestamps. `filters` is **deliberately schemaless** — adding a filter group later needs **no migration**. **Unique index on `(organisation_id, scope, lower(name))`** so the shared list can't collect three different things called "Kent"; the POST is an **UPSERT** (`ON CONFLICT … DO UPDATE`) matching that expression index, so re-saving a name **overwrites** rather than erroring — this was the owner's chosen duplicate-name behaviour. **Sharing boundary is the ORGANISATION** — there is no separate "sales team" grouping in WorkTrackr, and engineers can't reach Sales at all, so org == sales team in practice. **No money is stored or returned by this route**, so no commission/profit/pay role-gating applies. `isManagerish()` deliberately mirrors `isUnrestrictedRole()` in `sales-permissions.js` so "manager" means the same thing everywhere. Server **refuses to save an empty search** (it would be "show everything" under a name). **Frontend:** `CompanyFilterModal.jsx` gains a `SavedSearchMenu` drop-down in the header (click-away to close, shows name + the grey summary, per-row bin icon) and a **"Save this search"** name box above the footer, disabled until something is ticked, with inline success/error feedback; `CompanyPipelineList.jsx` does the fetch/save/delete and builds the summary. **The summary ("Voicemail + Contacted · Kent · No phone") is generated AT SAVE TIME from the on-screen labels and stored with the row** — generated later, a Spotter (stored as a UUID) could come back as "Unknown user" if that person's record changed, and a menu of bare names is unusable within a month. **`loadFilters()` was refactored to share a new `coerceFilters()`** which now defends **both** the browser-remembered filter **and** server-sent saved searches — a search saved before a filter group existed, or a tampered row, is rebuilt against the CURRENT shape and can never break the screen. The bin icon is shown **only** when the server would actually allow the delete (`isManager || createdBy === currentUser.id`), so it's never a dead button; the server enforces it regardless. Deleting asks for confirmation since it removes it **for everyone**. ⚠️ **`Dashboard.jsx` also changed** (one line) to pass `currentUser={user}` into `CompanyPipelineList` — it previously only received `isManager`, and the creator check needs the user id. **Verified:** `npm run build` passes, **1852 modules**, zero errors; `node --check` clean on both `saved-searches.js` and `server.js`; **migration SQL parsed with sqlglot as Postgres** (3 statements) and the **UPSERT with its expression conflict target parsed separately**; **37/37 new tests** (exact role matching — `administrator`/`co-owner`/`partner` correctly rejected; creator-vs-manager delete matrix incl. orphaned rows where the creator was deleted, and a null creator not matching a null user; empty-search guard incl. whitespace-only; and server-sent filters through `coerceFilters` incl. garbage/null/array/number/retired-group/wrong-type, plus a restored search still filtering correctly) **plus 129/129 prior tests still passing — 166 total, no regression**. `saved-searches`.
- **Website link on a company took the user to Tickets instead of the site** (frontend only — **NO backend change**; changed `CompanyProfile.jsx` only). Owner reported: clicking the website link on some companies (e.g. Thrive Gyms) gives a **white flash → "loading" → lands on Tickets**, while copy-pasting the same address into a browser tab works. **Diagnosed from the real code + a screenshot, not guessed. Full chain:** the stored value is a **bare domain with no `https://`** (`www.thrivegyms.co.uk`); `CompanyProfile.jsx` rendered it raw as `href={company.website}`; a browser treats a scheme-less href as **relative**, so it navigated to `worktrackr.cloud/app/www.thrivegyms.co.uk`; the server serves the SPA for any `/app/*` path (hence the genuine full page reload = white flash + "loading"); the app then doesn't recognise that route and falls back to its default view, which is **`currentView = 'tickets'`** (`AppLayout.jsx:75`, with `VIEW_TO_PAGE[currentView] || 'all-tickets'` at line 118). Every symptom accounted for. **Why only SOME companies:** there are two entry paths and only one tidied the URL — `AddCompanyPage.jsx:42` has a local `normalizeUrl()` that prepends `https://`, so **manually added** companies were always fine; the **CSV import** path (`routes/contacts.js`, bulk insert ~line 375, `String(row.website || '')`) stores the raw spreadsheet value, and most spreadsheets list sites as `www.…`. So imported = broken, manual = fine. Owner was offered three scopes (render-time fix only / also normalise on import / also backfill existing rows) and **chose render-time only** — it repairs every affected record instantly with no data change, no re-import and nothing to correct by hand. Added module-level `webHref()` (prepends `https://` unless an `http(s)://` scheme is already present, case-insensitive) and `webLabel()` (strips the scheme case-insensitively **and** any trailing slashes for display; the old inline `.replace(/^https?:\/\//, '')` was case-sensitive and would have left `HTTPS://` showing). Also null/undefined-safe, where the old code would have thrown on a non-string. **Security side effect, deliberate:** because the prefix is unconditional for anything without an http(s) scheme, a hostile value from a spreadsheet can no longer act as one — `javascript:alert(1)` becomes the inert `https://javascript:alert(1)`, and `//evil.com` can no longer be protocol-relative. `CompanyProfile.jsx` is the **only** place a website is rendered as a clickable link (grepped across all components), so this is a complete fix. ⚠️ **Still true and deliberately not fixed:** the CSV import continues to store bare domains, so the underlying data stays untidy — harmless now that the link is fixed at the point of use, but if a future screen links a website it must use `webHref()` too, or the bug returns there. Note `domainFromWebsite()` (dedupe) already strips schemes itself, so import dedupe is unaffected either way. **Verified:** `npm run build` passes, **1852 modules**, zero errors; **36/36 new link tests** (the exact Thrive Gyms case, bare domains with/without www and with paths, an explicit assertion that output can **never** be relative, already-good and mixed-case schemes left untouched, whitespace/empty/null/undefined, the `javascript:`/`data:`/protocol-relative/`ftp:` neutralisation cases, and the label's scheme + trailing-slash stripping) **plus 93/93 prior filter, address and persistence tests still passing — 129 total, no regression**. `website-link-fix`.
- **Companies filter now PERSISTS — survives opening a company** (frontend only — **NO backend change**; changed `CompanyPipelineList.jsx` only). Owner reported: pick filters → click a company in the results → click back out → **filters have reset**. **Root cause found by reading the real code, not guessed:** `Dashboard.jsx` (~line 456) doesn't overlay the company profile, it **swaps the component out** — `openCompanyId ? <CompanyProfile …> : <CompanyPipelineList …>` — so the entire list screen **unmounts** and every piece of its state is destroyed, then rebuilt from defaults on the way back. This is also exactly why the pipeline/list **view toggle already survived**: it was the only thing saved to the browser (`wt_companies_view`). Fix follows that existing in-file pattern rather than inventing a new one: `wt_companies_filters` (JSON of the whole `filterSel`, so stage badges + all 10 tick-box groups + `addressQuery` all persist) and `wt_companies_search` (the top text box). Both restored via lazy `useState` initialisers (`loadFilters` / `loadSearch`) and written back by `useEffect` on change; every `localStorage` call is wrapped in try/catch like the existing code, so private mode or blocked storage degrades to "doesn't remember" rather than crashing. **`loadFilters()` deliberately REBUILDS the object against the current `EMPTY_FILTERS` shape instead of trusting what it reads** — a stale, partial, wrong-typed or corrupted saved value would otherwise crash the Companies screen on mount, and a user would have no way out of that (they can't clear their own localStorage). Non-array groups, non-string entries inside a group, a non-string `addressQuery`, and keys from filter groups that no longer exist are all discarded; garbage/truncated JSON, `null`, arrays and primitives all fall back to `EMPTY_FILTERS`. **"Clear filters" is now the single reset** (`clearFilters()`) and clears the top search box **as well as** the tick boxes — matching the owner's stated mental model that the list stays put "until the user clicks Clear filters". The link therefore now also appears when only the search box has content (previously it keyed off `activeFilterCount` alone). The archive toggle reuses `clearFilters()` too. Note the `Filter · N` count still counts **only** the pop-up/badge selections, not the search box, since the search box isn't in the pop-up and its content is already visible on screen. **Verified:** `npm run build` passes, **1852 modules**, zero errors; **23/23 new persistence tests** (round trip incl. a restored filter still matching a company; garbage/truncated/`null`/array/string/number saved values; partial and wrong-typed objects repaired; unknown groups dropped; shape always equals current `EMPTY_FILTERS`; and a `localStorage` that throws on both read and write) **plus 33/33 filter + 37/37 address tests still passing — 93 total, no regression**. `filter-persist`.
- **"Address contains" search — filter companies by county/town/postcode** (frontend only — **NO backend change**, **NO new field**, **NO migration**; changed `CompanyFilterModal.jsx` + `CompanyPipelineList.jsx`). Owner wanted to filter by county ("essex or london or kent") and the app **has no county field** — the whole address is a single free-text line in `contacts.addresses[0]`. Investigated first and found two things: (1) the importer maps only **ONE** spreadsheet column to Address, so separate Town/County/Postcode columns would be silently dropped, and (2) **the owner confirmed their CSVs never had a county column at all** — so county exists only inside the address string, and a proper `crm.county` field would be blank on all ~9,000 records with nothing to backfill from except the address text. Three options were put to the owner: (a) auto-derive areas by tallying recurring comma-separated address parts (global, no hardcoded county list), (b) free-text "Address contains" box, (c) a real County/Region field + re-import. **Owner chose (b).** Implementation: new `addressQuery` string key on the shared `filterSel` state (`EMPTY_FILTERS` now spreads the group arrays **plus** `addressQuery: ''`), rendered as a text box at the TOP of the Filter pop-up via a new generic `textFields` prop on `CompanyFilterModal` (`[{key,label,placeholder,hint}]` — kept generic so more text filters can be added later without touching the modal). **Comma-separated terms mean ANY of them** — `'essex, london, kent'` matches all three, which is exactly the owner's ask. **Matching is WHOLE-WORD** (`(^|[^a-z0-9])term([^a-z0-9]|$)` on a lowercased haystack, term regex-escaped via `escapeRe`) — this is the important bit: a plain substring match would have made "Kent" also return every **Kentish Town** address in London. Word edges mean punctuation/hyphens still work (`Stratford-upon-Avon` matches `avon`) and terms at the very start/end of the string match. `addressTextOf()` flattens **all** address entries and tolerates **both** stored shapes (plain string and legacy `{line1,city,postcode,…}` object), same tolerance as `hasAddress()`/`addressText()`. A company with **no address is excluded** when a term is typed (it can't be in Kent if it has no address) but is unaffected when the box is empty. `matchesAddressQuery` is called at the TOP of `matchesFilters` so it applies **even when a group is skipped** via `skipKey` — i.e. the stage badge counts respect the address search. `activeFilterCount` had to change from a plain array-length sum to an entry-wise reducer so the text box counts as 1 toward the `Filter · N` badge; the modal's `totalChosen` and `clearAll` likewise handle text keys. Enter inside the box applies the filter. **Deliberately NOT country-specific** — no county/state list is hardcoded anywhere, so it works for Texas, Bayern, Ontario etc. (tested). ⚠️ **Known limits, accepted:** it can only find what is actually written in the address, so a Kent company whose address omits "Kent" won't be found; and a street literally named after a place (e.g. "Essex Road, London") will match that term. If the owner later wants exact ticking, the route is a real `crm.county` field (**must** be added to the zod schema in `contacts.js` or it silently vanishes) plus an import mapping. **Verified:** `npm run build` passes, **1852 modules**, zero errors; logic extracted verbatim from source and unit-tested — **37/37 new address tests** (incl. the Kentish-Town guard, comma-OR, empty/whitespace/commas-only queries, no-address and null-addresses, postcode + town matching, accented `München`, regex-special characters, object-shaped addresses, multiple address entries, and AND-combination with the tick-box groups) **plus 33/33 prior filter tests still passing** (no regression). `address-county-search`.
- **Companies list — advanced tick-box Filter pop-up** (frontend only — **NO backend change**; one **NEW** file `CompanyFilterModal.jsx`, plus `CompanyPipelineList.jsx`). ⚠️ **Continuity note:** `_live_state` was MISSING at the start of this session (outputs folder empty). Verified directly against the real code that the uploaded zip **already contained every change through `company-spotter`** (spotter zod line + dropdown, voicemail enum, NO_STAGE sentinel, MyTasks `ymd()`, dedupe `seenDomain/seenPhone`, `/logout` intercept, both picker components) and that the baseline build passed at **1851 modules**. Snapshot re-seeded from it. Owner asked for a search filter with tick boxes for "every field and every badge type", as a tidy pop-up, button **next to the badges**. Owner's three choices: **list view only** (matches where the badges live today), **all 10 groups**, and **keep the existing badges as quick shortcuts** with the pop-up as an extra. **Design:** the stage badges and the pop-up's Stage group are now **one shared piece of state** (`filterSel`, an object of arrays, `EMPTY_FILTERS` per group) so the two can never disagree — a badge click toggles that stage in the array, and badges highlight for whatever the pop-up selected. This makes the badges **multi-select**, which is the headline win: Voicemail **+** Contacted now shows both (before, clicking one replaced the other). "All" clears the stage array only. **Within a group ticks are OR, across groups AND** (`matchesFilters`, which takes a `skipKey` so `filtered` excludes the stage group and the badge counts stay meaningful while a stage is ticked). **The 10 groups** (`GROUP_DEFS`): Stage, Source, Industry, Employees (`crm.companySize`), Account manager (`crm.assignedTo`), Spotter (`crm.spotterUserId`), Customer status (`crm.status`), Tags, Missing details, Chase date. **Every option is derived from the loaded companies** via `valuesFor(co, key)` and tallied against the search-filtered base set (`searched`) — options with a **zero count are dropped and an empty group is hidden**, so the user can never tick something that matches nothing and nothing is invented. Counts are measured against `searched` (text search only) so the numbers **don't jump around as you tick**. Two groups are **derived, not stored**: **Missing details** (no phone / no email / no website / no address) and **Chase date** (Overdue / Due today / Upcoming / No chase date, via `chaseBucket`). `hasAddress()` tolerates **both** stored address shapes (plain string and legacy `{line1,city,postcode}` object) — same tolerance as `addressText()` in `CompanyProfile.jsx`. **Spotter** stores a UUID, so the list fetches the existing `GET /api/tickets/users/list` (returns `{users:[{id,name,email,role}]}`, verified) to show readable names; an id with no matching user renders "Unknown user". Ordering: Stage/Missing/Chase use a **fixed** order (`FIXED_ORDER`), everything else is alphabetical by label. **The existing "All sources" dropdown was NOT removed** — it now writes to the same shared `sources` array (single pick sets `[v]`, "All sources" clears); if the pop-up selects 2+, the dropdown shows a read-only "N sources selected" marker so the two controls never lie to each other. Pop-up UX: groups in a responsive 1/2/3-column grid, each group scrollable (`max-h-40`) with a **type-to-narrow box once a group exceeds 8 options** — needed because Industry can run to hundreds of values across ~9,000 imported companies. Draft state means **Cancel discards and only Search commits**; Clear all inside, plus a `Filter · N` count on the button and a **Clear filters** link beside it. Empty-state message is now filter-aware ("No companies match your filters") instead of naming a single stage. Archive toggle resets all filters. **Verified:** `npm run build` passes, **1852 modules** (+1, the new component), zero errors; and the filter logic was extracted verbatim from the source and unit-tested — **33/33 pass** covering stage bucketing (missing/empty/junk → No stage), blank-vs-set text fields, both address shapes, chase buckets incl. **full ISO timestamps** (what the API actually returns, the same trap that caused the MyTasks "Invalid Date" bug), OR-within/AND-across, `skipKey`, and companies with no value in a filtered group being correctly excluded. `company-filter-popup`.
- **"Spotter" dropdown on the company record** (**includes ONE backend file** — `web/routes/contacts.js`). Owner wanted to record which salesperson spotted/found a company, chosen from a dropdown of ALL users (incl. future-added), placed **between Source and Next action** on the Stage/Source strip, and it must persist. Stored as `crm.spotterUserId` (the user's UUID). ⚠️ **Needed a backend line:** the `crm` zod object **strips unknown keys** (verified empirically — a stray `spotterUserId` was silently dropped), so without the schema addition the dropdown would have been a dead control. Added `spotterUserId: z.string().uuid().optional().nullable()` to the crm schema. Frontend (`CompanyProfile.jsx`): loads users once via the existing `GET /api/tickets/users/list` (org-scoped, ordered by name, returns `{id,name,email,role}` — re-queried live so **future users appear automatically**); new `setSpotter()` mirrors `setSource()` and calls the existing `saveCrm()` (merges `crm`, PUTs `/api/contacts/:id`); dropdown reuses the strip's `inputStyle`, blank option "—" clears it (`spotterUserId || null`). Persistence verified: valid UUID kept, null clears. No migration (lives in `contacts.crm` JSONB). `company-spotter`.
- **Task pop-up reworked notes-first + "Invalid Date" fix** (`MyTasks.jsx` only, frontend). Owner: the pop-up's dropdowns took too much room and the notes/text area was too small — "the point of this window is for notes." Redesign: modal widened to `max-w-2xl`, height `max-h-[88vh]`, `flex flex-col`; the **Task/notes textarea is now `flex-1 min-h-[220px] resize-none`** so it fills all remaining height and grows with the viewport; the five fields (Company, Assigned to, Due date, Priority, Status) moved into a compact `grid-cols-2 sm:grid-cols-3` block with small 11px labels and `py-1.5` controls, so they sit in two tight rows below the notes instead of dominating. **Also fixed a real bug** spotted in the screenshot: the Due date read **"Invalid Date"** because a saved task's `dueDate` comes from the API as a full ISO timestamp (`2026-07-31T00:00:00.000Z`) but the shared `DatePicker` only parses plain `'YYYY-MM-DD'` (it does `new Date(\`${value}T00:00:00\`)`). Added `ymd()` normaliser used in `openEdit` — takes the first 10 chars of an ISO string, or formats a fallback date, else ''. Verified across ISO/clean/empty/null. Left the date broken would have let Save write a bad/blank due date. `task-popup-notes-layout`.
- **Task pop-up (view/edit/delete) + multi-line task text + logout redirect** (frontend only — **NO backend change**; PUT/DELETE `/api/tasks/:id` already supported every field). Context: owner hit a session-wide **401** saving tasks — root cause was the **7-day auth cookie expiring mid-session** (issued ~24 Jul, expired ~31 Jul). 401 (not 403) because an expired *cookie* isn't sent at all → middleware `authenticateToken` returns 401 "Access token required" (403 is only for a present-but-invalid token). Re-login fixed it (confirmed). There's **no session refresh and no sliding renewal** — a flat 7 days — and **no global 401→login handler** (only the `!user` guard in `App.jsx` redirects). Those two durable gaps are NOT yet fixed (offered). This turn fixed the 3 follow-on issues: (1) **Logout didn't reach the login screen** — the sidebar Logout routes `handleNav('/logout')` → `onNavigate('/logout')`, which is not a real route so the app's catch-all bounced it back to the dashboard; it never called the real logout. Fixed in **`AppLayout.jsx` `handleNavigation`**: intercept `view === '/logout'` → `POST /api/auth/logout` (clears cookie) then `window.location.href = '/login'` (hard reload clears all state; `/login` is a real route in `main.jsx`). Chose the hard redirect over `useAuth().logout()` because there are TWO AuthContexts in the tree (`App.jsx`'s own + `context/AuthProvider.jsx`) and the hard path is provider-agnostic and bulletproof. (2) **Pasted multi-line list collapsed to one line** — the create form's "What needs doing?" was a single-line `<input>` (strips newlines on paste). Changed to a `<textarea>` (rows=3, preserves lines). List rows show only `firstLine(title)` + a faint "＋more" when multi-line (`isMultiline`), full text in the pop-up. (3) **Clicking a task did nothing** — rows had no open action. Added a **pop-up modal** (`editForm` state, `openEdit`): click a row opens it (checkbox `stopPropagation` so ticking doesn't open it); edit multi-line **Task text**, **Company**, **Assigned to**, **Due date** (shared `DatePicker`), **Priority**, **Status**, with **Save** (`PUT`, patches local state from the returned row), **Delete** (`DELETE`, with confirm), Cancel/close-on-backdrop. **Status is only To do (`open`) / Done (`done`)** — the DB has a CHECK constraint `status IN ('open','done')`, so no other statuses without a migration; "Overdue" stays a derived label, not a stored status. `task-popup-logout`.
- **Import duplicate-detection widened to name + domain + phone** (**includes ONE backend file** — `web/routes/contacts.js`; other changed file `CsvImport.jsx`). Owner asked what stops duplicate imports and wanted name/domain/phone checked. **Before:** only (a) in-file merge on exact lowercased company NAME, and (b) skip-vs-existing on exact lowercased NAME **or** exact lowercased EMAIL. No phone, no domain, no DB unique constraint anywhere. **After** (owner chose *strictest*: auto-skip on name, domain OR phone; and *keep exact name matching* — no Ltd/Limited/punctuation normalisation): a row is a duplicate if its **name** (exact, case-insensitive) OR any **domain** OR its **phone** matches — applied BOTH in-file (frontend, so the preview count is right) AND against existing WorkTrackr records (backend, the real gatekeeper). **Domain** = bare registrable domain from the website (`http://www.acme.co.uk/x` → `acme.co.uk`) plus the email's domain **only if not a free provider** (gmail/outlook/btinternet/etc. blocklist — otherwise every gmail contact collapses into one company). **Phone** = digits-only, UK-aware (`+44 1233 539501`, `01233 539501`, `01233539501`, `1233539501` all collapse to `01233539501`); anything under 10 digits (extensions, `n/a`, `0`) is ignored as a key so it can't over-merge. The domain/phone/generic-list helpers are **duplicated verbatim** in `CsvImport.jsx` (module scope, `domainsOf(obj)`) and `contacts.js` (inside `/import`, `domainsOf(website,email)`) — logic verified byte-identical bar one trailing comment; **if you change one, change the other** or the preview and the import will disagree. Backend `existing` SELECT now also pulls `website, phone`; cross-batch still safe (re-read per request) and within-batch `seenDomain`/`seenPhone` added alongside the existing name/email sets. On-screen wording updated: "repeated rows merged (same name, website or phone)" and "already in WorkTrackr (matching name, email, website or phone)". ⚠️ **Accepted trade-off (owner's call):** two genuinely separate businesses sharing a switchboard number or one website (franchises, umbrella groups) will be treated as duplicates and the second skipped. Verified end-to-end: same company entered 3 ways (via web domain, via shared phone) correctly merges to 1; a genuinely new company with a gmail address is NOT falsely merged. `dedupe-domain-phone`.
- **"No stage" filter chip + new "Voicemail" stage** (**includes ONE backend file** — `web/routes/contacts.js`). (a) **No-stage chip** on the Companies **list view**, sitting between "All" and "Suspect", showing a live count and filtering to companies with no stage. Added to `CompanyPipelineList.jsx`: a `NO_STAGE='__nostage__'` sentinel + `isNoStage(co)=!STAGE_BY_KEY[co?.crm?.salesStage]` (matches EXACTLY the rows that render the grey "No stage" pill — missing, empty, or unrecognised salesStage). `listVisible` handles the sentinel before the normal equality branch; `noStageCount` computed off `filtered`; also hardened the existing `counts` to only tally **known** stage keys (`if (STAGE_BY_KEY[k])`) so a stray value can't inflate a chip. Verified logic: null+empty+junk all counted as No-stage, and every bucket (All = no-stage + each stage) sums to the total. Chip only renders in list view (matches where the other chips live). (b) **Voicemail stage** added to the ladder: **Suspect → Contacted → Voicemail → Prospect → Hot prospect → Customer** (cyan pill `#06b6d4` / `rgba(6,182,212,0.20)`, distinct from the others). Same full rollout as Contacted — **not frontend-only**: `contacts.js` zod enum + `/import` `VALID_STAGES` both now include `'voicemail'` (verified zod accepts `voicemail`, still rejects junk), else it'd be a dead control. Frontend stage lists updated in all four LIVE screens (`CompanyProfile.jsx`, `CompanyPipelineList.jsx` incl. pill colour, `AddCompanyPage.jsx`, `CsvImport.jsx`); pipeline kanban maps over `STAGES` so a Voicemail column auto-appears (kanban now 6 columns — wraps to a 2nd row on wide screens, not broken). Placement of Voicemail is a one-line reorder if the owner wants it elsewhere. ⚠️ **Continuity note:** the uploaded zip is the ORIGINAL code; the owner's live app already has all prior changes applied. This turn was rebuilt on top of the prior handed-over files (from outputs) — and `CompanyPipelineList.jsx`'s turn-3 "Contacted" change had to be **re-applied** first because it wasn't in the last output package. Any future turn must reconstruct the live state the same way, not edit the raw zip. `nostage-chip-voicemail`.
- **Address + Industry + Employees are now REAL fields on the company record, and importable** (**includes ONE backend file** — `web/routes/contacts.js`). Owner needed to import a 257-row suspects export (`Company, Name, Phone Number, Address, Number of Employees, Industry`) and asked whether an address field existed. **It did not** — `contacts.addresses` (JSONB) existed in the DB + zod schema and `crm.industry` / `crm.companySize` existed in the crm schema, but **NO reachable screen rendered or edited any of them** (the company card showed only phone/email/website; the address-aware `QuickAddCustomerModal` is imported nowhere and `ContactManager` has no sidebar entry). So half that spreadsheet had nowhere to land. **No migration and no schema change were needed** — the slots already existed; only the UI and the `/import` INSERT were missing. Changes: (1) `CompanyProfile.jsx` — company-details card now shows **Address** (`MapPin`), **Industry** (`Building2`) and **employee count** (`UsersRound`) with empty-state text, and the existing pencil edit-form gained those three inputs; `saveDetails` now also sends `addresses: [text]` and a **merged** `crm` (must merge — the PUT replaces the whole `crm` column). New `addressText()` helper reads **both** stored shapes (plain string or `{line1,city,postcode,…}` object) so any legacy data still renders. (2) `AddCompanyPage.jsx` — same three captured when adding a company by hand. (3) **backend `/import`** now inserts `addresses` and folds `industry`/`companySize` into `crm` (INSERT went 11→12 columns; column/placeholder/value counts verified 12/12/12). (4) `CsvImport.jsx` — three new mappable fields (`address`, `industry`, `employees`). Matching notes: bare `location` is deliberately only a **weak** hint while `company location` is **exact**, so LinkedIn exports pick `company_location` (the company) over `location` (the person); `company_employee_range` is excluded via the `range` avoid-term so `company_employee_count` wins. (5) **Bare-`Name`-column fix** — `'name'` added to `primaryContact.exact` so a "Name" column maps to Contact person, plus a **safety net**: if Company name ended up unmapped and Contact person holds a single bare column, it is moved to Company name (required field wins) — so a file whose only name column is "Name" still imports. (6) **UK phone repair** (`fixUkPhone`, tick-box ON by default, shows the count it would change): Excel strips the leading zero from UK numbers, so `01892807001` arrives as `1892807001`. Restores it **only** when unambiguous — bare 10 digits starting 1/2/3/7/8/9. Anything starting `0`, `+` or `00`, any other digit count, and any non-numeric value is left byte-for-byte alone (edge-tested: `+44…`, `0044…`, `441892807001`, `555`, `4123456789`, `n/a`, `ext 204` all untouched). Verified on both real files — **suspects: 257 rows → 247 companies, 124 phones fixed, all 6 columns mapped automatically**; **LinkedIn regression: still 9,038, now also filling address/industry/employees**. `address-industry-employees`.
- **New "Contacted" sales stage + notes show the full date** (**includes ONE backend file** — `web/routes/contacts.js`). (a) **Contacted stage.** New ladder: **Suspect (`new`) → Contacted (`contacted`) → Prospect → Hot prospect → Customer**. ⚠️ **This could NOT be frontend-only:** `contacts.js` validates `crm.salesStage` with a **zod enum** (`contactSchema`, used by `POST /` at ~L329 and `PUT /:id` at ~L389 via `.partial().parse()` — note `.partial()` does NOT relax the *nested* `crm` enum, verified empirically), so a UI-only option would have thrown a validation error on save and silently rolled back = a dead control. Backend edits: zod enum `salesStage` + `VALID_STAGES` in the `/import` route, both now include `'contacted'` (+2 stale comments corrected). **No migration needed** — `salesStage` lives in the `contacts.crm` JSONB with no DB CHECK constraint, and `GET /api/contacts?stage=` passes the value straight through unvalidated, so filtering by the new stage works with no further change. Frontend stage lists updated in the **live** screens only: `CompanyProfile.jsx` (the record's Stage dropdown), `CompanyPipelineList.jsx` (pipeline column + filter chip + pill; new violet `#8b5cf6` / `rgba(139,92,246,0.20)` colour, distinct from the existing grey/blue/amber/green), `AddCompanyPage.jsx`, `CsvImport.jsx` (fixed-stage list). **Deliberately NOT touched — dead/unreachable:** `LeadsList.jsx` + `AddLeadModal.jsx` (Leads tab removed from `SalesTabs.jsx`; tabs are Companies · Quotes · Orders · Calendar) and `CompanyNotes.jsx` (view `company-notes` is in `AppLayout`'s map but in **no** sidebar item). If any is ever revived, add `contacted` to its stage list. (b) **Note/file stamps now always carry the date.** `stamp()` in `CompanyProfile.jsx` previously rendered today's items as just `Today 14:36` with no date. Now: `Today · 24 Jul 2026, 14:36` / `Yesterday · 23 Jul 2026, 09:05` / `16 Jul 2026, 10:04`. Locale-driven (`undefined` locale = browser default, unchanged), so it reads as UK format for the owner; hover still gives the full locale string. Affects the **History & notes** timeline and the **file-attachment** list (same helper — consistent, intentional). `contacted-stage-note-dates`.
- **CSV import rebuilt for big real-world spreadsheets** (`CsvImport.jsx`, frontend only — **no backend change**). Owner is importing ~56,000 lead records across 6 LinkedIn/Sales-Navigator-style exports (43 columns, one row per *person*, ~10k rows/file). Four real problems fixed: (a) **auto-match was wrong** — `guess()` did a naive `header.includes(hint)` + `findIndex`, so `firstname` won "Company name" (contains "name") and the near-empty `email` column (5 of 10,000 filled) beat `email_enrich` (4,393). Replaced with a **scored matcher**: `norm()` a heading (`company_website`→`company website`), score exact=100 / prefix=80 / suffix=70 / whole-word=60 / weak-substring=20, minus 60 for an `avoid` term; auto-map only at **score ≥ 50**; where two candidates are **within 25 points, the better-FILLED column wins** (that's what flips `email`→`email_enrich`). Verified on the owner's real file: Company name→`company`, Email→`email_enrich`, Phone→`phone`, Website→`company_website`, Stage/Notes→skip. (b) **The Import button was effectively DEAD at scale** — the server caps at **5,000 rows** (`rows.length > 5000` → 400) but the screen offered "Import 10000". Now sends in **batches of 500** with a progress bar, so any size works and no single request can time out; totals are accumulated across batches. **Cross-batch dedupe is safe** because the route re-reads existing contacts per request. Partial failure reports how many got in and says re-running is safe (server dedupes). (c) **Two columns can be JOINED** into one field (`col2`, values joined with a space) — needed because contact names arrive as separate `firstname`/`lastname`; if no single "contact" column is found, `findNamePair()` auto-joins them. (d) **A field can take one FIXED value for every row** (`__fixed__` sentinel; `salesStage` renders a dropdown of the 4 valid stages) — otherwise 9,000 companies land with "No stage". Plus: **junk-row filter** (on by default — `Self-employed`/`Freelance`/`Retired`/blank/1-char names), **in-file dedupe by company name** before sending, a **preview table** of the first 5 mapped rows, a **"Reading…" state** (the 30 MB parse blocks ~7s; a `setTimeout` lets it paint first), and the **mapping is remembered** in `localStorage` `wt_csv_import_map` **by column NAME** (not index) so spreadsheets 2–6 auto-fill. **The CSV parser itself is byte-for-byte unchanged** (proven on the real file — 10,001 rows, 43 cols). Props (`onBack`/`onDone`) unchanged, so both callers still work. Live callers: `CompanyPipelineList.jsx` (Companies → Import) and the deprecated/unreachable `LeadsList.jsx`. Real numbers on file 1: 10,000 rows → **9,038 companies**, 880 repeats merged, 45 junk, 37 nameless, 19 batches. `csv-import-bulk`.
- **Date + time pickers — final batch (date-picker rollout COMPLETE).** NEW `DateTimePicker.jsx` (exports default `DateTimePicker` for `datetime-local`-shaped fields — value/onChange are `'YYYY-MM-DDTHH:MM'` strings — plus named `TimePicker` for `'HH:MM'` fields). **Time is always 15-min steps (00/15/30/45)** via the `TIME_OPTIONS` list; no per-minute native spinners anywhere reachable. Both match `DatePicker`'s dark pop-up and accept `className` + inline `style`. `DateTimePicker` = `DatePicker` (flex-1, wraps) + `TimePicker`. Swapped every **live** native field: `CRMCalendar.jsx` (create date+time → DateTimePicker; Book-Meeting / Schedule-Meeting / Edit dates → DatePicker; their time fields were already 15-min `Select`s), `CreateTicketModal.jsx` (scheduled_date datetime), `JobForm.jsx` (scheduled_start/end — JobForm IS live via a route in `app/src/App.jsx`), `VoiceAssistant.jsx` (3 datetime + 1 date + 2 time), `CompanyProfile.jsx` (3 `type="time"` → TimePicker), `XeroIntegration.jsx` (invoice date — was **uncontrolled** `defaultValue`; added `invoiceDate` state). Remaining native date/time inputs live ONLY in **dead/unrendered files** (`SafetyTab.jsx`, `*_broken`, `CreateTicketModalFixed`, `EnhancedCreateTicketModal`, `TicketDetailView` non-Tabbed, `BookingCalendar`, `IntegratedCalendar`) — skip unless one is ever wired up. `datetime-picker-all`.
- **Health & Safety date-pickers.** The live H&S screen is `SafetyTabComprehensive.jsx` (rendered by `TicketDetailViewTabbed.jsx` as `SafetyTab`); the standalone `SafetyTab.jsx` is **dead/unimported** — ignore it. All 11 date fields go through the module-level `FormField` abstraction, so a single new `type === 'date'` branch in `FormField` (rendering the shared `DatePicker`, `onChange={(v) => updateFormData(name, v)}`) converted **all 11** at once (Date, Assessment/Review/Action/Service/Effective dates, etc.). `hs-datepickers`.
- **Admin dashboard light-theme fix + ticket date-pickers.** (a) **Admin fix:** same white-body-text leak the public pages had also hit the light admin pages — wrapped `/admin87476463/dashboard` (`AdminDashboard.jsx`) and `/admin87476463/users/:userId` (`UserDetailPage.jsx`) in the `.wt-public` guard in `main.jsx` (AdminLogin is intentionally dark, left alone). (b) **Ticket day-pickers:** confirmed the **live** ticket screens are `CreateTicketModal.jsx`, `TicketDetailModal.jsx`, `TicketDetailViewTabbed.jsx` (the `Enhanced*`/`*Fixed`/`TicketDetailView`/`*_broken` variants are **dead/unrendered**). Swapped the plain `type="date"` (scheduled_date) in **TicketDetailModal** and **TicketDetailViewTabbed** to the shared `DatePicker`. Both screens' `onChange = (field) => (e) => { const v = e?.target ? e.target.value : e; ... }` already accept a raw value, so `onChange={onChange('scheduled_date')}` works directly. `CreateTicketModal`'s field is a **`datetime-local`** (date+time) → left for the date-time batch. `admin-theme-and-ticket-datepickers`.
- **Two separate calendars — Sales vs Delivery** (`CRMCalendar.jsx` + `Dashboard.jsx`). Both the Delivery "Calendar" (sidebar, view `calendar`) and the Sales "Calendar" tab (view `sales-calendar`) render the **same `CRMCalendar` engine** (so both already got Pass 1 + Pass 2 behaviour); this change makes them genuinely two calendars by **content** and by **what "Add" creates**. New prop `calendarKind` (`'sales'` default | `'delivery'`), `isDelivery` derived. Dashboard now passes: Delivery = `calendarKind="delivery"` + `defaultSources={{sales:false,projects:true,schedule:true}}` (holidays stays on); Sales = `calendarKind="sales"` + sales-only sources (`crm-calendar` view left on the `'sales'` default). On the **Delivery** calendar: "Add Activity" becomes **"Add Entry"** and creates a **calendar_events** schedule entry (`POST /api/calendar/events`) instead of a CRM event; the sales-only "Book Meeting" button and the Type/Company/Contact fields are hidden. Schedule/`calendar_events` entries are **no longer read-only** — they open a "Calendar Entry" detail modal and can be **edited** (`PUT /api/calendar/events/:id`) and **deleted** (`DELETE /api/calendar/events/:id`); scheduleItems now carries `_calId` (the real row id, since the calendar id is prefixed `cal-`), and `deleteEvent`/`saveEditedEvent` branch by `_isSchedule`. All calendar endpoints **already existed** — no backend change. Both calendars keep the source-toggle chips, so anyone can still flip on a combined view. **The other two calendar files (`IntegratedCalendar.jsx`, `BookingCalendar.jsx`) remain dead/unwired** — not rendered anywhere. `calendars-split-sales-delivery`.
  - Minor/known: editing a schedule entry reuses the shared edit modal (sales-only fields hidden); its Date field is still a native `type="date"` (part of the later date-picker date+time batch). Tickets/jobs on the Delivery calendar open as before (ticket screen / project).
- **Calendar interactions — Pass 2 of 2 (feature complete)** (`CRMCalendar.jsx`). (a) Each day now shows **all** its entries (removed the Month "+2" / Week "+3" caps — cells grow taller on busy days, per owner's choice); (b) **same-time entries render side-by-side** via a new `groupByTime` helper (buckets an already-time-sorted day by displayed time; each bucket is a flex row so same-time items split the width, `min-w-0 truncate`); applied in Month, Week and Day views; (c) **clearer colours** — each `eventTypes` entry gained a solid `accent` hex and every entry (grid chips, day cards, selected-day sidebar) now carries a left colour-bar in its type colour (Call=blue, Meeting=green, Follow Up=purple, Renewal=orange, Project=amber, Ticket=indigo, Schedule=slate, Holiday=teal). **Confirmed CRMCalendar is the only live calendar** — `IntegratedCalendar.jsx` and `BookingCalendar.jsx` exist but are **not rendered by any screen** (dead/unwired), so this covers every reachable calendar; if either is ever switched on, bring it in line then. `calendar-showall-pass2`.
- **Calendar interactions — Pass 1 of 2** (`CRMCalendar.jsx`, the ONLY live calendar — `IntegratedCalendar.jsx`/`BookingCalendar.jsx` are imported but never rendered; ignore them). Owner wants: click empty space → new entry; click an entry → open it; day's entries time-ordered; same-time entries side-by-side + clearer colours. **Pass 1 done:** (a) `getEventsForDate` now returns each day **sorted earliest-first** by start time (new `sortByTime` helper); (b) new `openCreateForDate(day, time='09:00')` opens the existing "Add Activity" modal pre-filled to the clicked day — wired to **empty-space clicks** in Month (day cell), Week (day column) and Day (dashed "add" affordance + clickable empty state); (c) entries now **open on click in Month** too (chips were previously non-clickable) — chips use `e.stopPropagation()` so an entry-click doesn't also fire the cell's create; "+N more" selects/opens that day instead of creating. Read-only items (staff **holidays**, work-**schedule** entries) still don't open an editable form (unchanged `openEvent` behaviour) — flagged to owner. `calendar-clicks-pass1`.
  - **🔜 Pass 2 (NOT started):** show **all** of a day's entries (remove the Month "+2"/Week "+3" caps; cells grow), render **same-time entries side-by-side** (two columns), and make entry **colours** clearer/more distinct. Type colours already come from `getEventTypeConfig(type).color` (`eventTypes` near the top of the file) — extend/apply these.
- **Public front-pages theme fix** — the dark in-app stylesheet (`src/app/src/App.css`) is bundled **globally** (it's reached via `main.jsx → AppEntrypoint → src/app/src/App.jsx → './App.css'`, all static imports, and the build emits **one** combined CSS file), so its `body { background:#1a1a2e; color:#fff }` base rule + dark `:root` colour tokens also landed on the **light** public pages — turning headings and button labels invisible (only text/tokens with an explicit colour survived, e.g. `text-gray-600`, the feature-card titles). Fix: a `.wt-public` **light-theme guard** in `src/App.css` (re-asserts the original light `:root` tokens + dark `color` for its subtree; custom props resolve from the nearest ancestor so the wrapper wins), and each public route in `main.jsx` is wrapped `<div className="wt-public">…</div>` (landing `/`, `/pricing`, `/signup`, `/login`, `/welcome`, `/forgot-password`, `/reset-password`). The dark app is **untouched**. `public-pages-theme-fix`.
  - ⚠️ **Not yet fixed (same root cause):** the secret **admin dashboard** (`admin/AdminDashboard.jsx`) is also a light page (`bg-gray-50`) and will show the same washed-out text; wrap it in `.wt-public` too if/when the owner wants it. (`AdminLogin.jsx` is intentionally dark and is fine.)
  - Deeper option (flagged, not done — higher risk to the working dark app): scope the dark app's global `body{}`/`:root{}` under an app-root class instead of guarding each public page.
- **Date-picker Phase 2 (plain day-pickers)** — extended `DatePicker.jsx` with an optional `style` prop (additive; Phase-1 screens unaffected) + swapped 8 date boxes across 4 screens: `AddLeadModal.jsx` (2), `CompanyProfile.jsx` (4 date halves; time boxes left), `CRMDashboard.jsx` (renewal date), `InvoiceDetail.jsx` (due date). `datepicker-phase2`.
- **Date-picker Phase 1** — shared `DatePicker.jsx` + 7 screens (above). `datepicker-phase1`.
- **My Notes full-width** — removed a `max-w-3xl mx-auto` cap in `PersonalNotes.jsx`. `notes-full-width`.
- **Plan/billing moved to Pricing** — the `PlanManagement` block (plan tiers + Additional Seats + Delete-Account Danger Zone) moved from User Management to the **Pricing** page (`Dashboard.jsx` renders it for `pricing-config`, admin-only; removed from `UserManagementImproved.jsx`). `plan-moved-to-pricing`.
- **Delete-user actually works** (**backend**) — the bin only removed from local state before. Added admin-only `DELETE /api/organizations/:id/users/:userId` (`organizations.js`) that removes the membership + deletes the person's org-scoped personal data (holiday allowances/requests/adjustments, `user_sales_permissions`, `personal_notes`, `engineer_wage_records`, `commission_period_locks`) via best-effort per-table try/catch, and **preserves the `users` row + all tickets/history** (tickets FK is RESTRICT, so the row must stay). Frontend now confirms + calls the server + only removes on success. `user-delete-fix`.
- **Holiday admin 500 fix** (**backend**) — `/staff` query filtered `m.status='active'` but the **live `memberships` table has no `status` column** (schema.sql lists it; live DB predates it — trust the live DB). Removed the clause. `holiday-staff-fix`.
- **Per-user Sales permissions (full feature)** — control which Sales areas each staff member can access. (a) `104_create_sales_permissions.sql` (**backend NEW**, `user_sales_permissions(org,user_id,perms jsonb)`) + `sales-permissions.js` (**backend NEW**, `/me`, admin `/:userId` GET+PUT, exports `effectiveFor`; defaults seeded from role so nobody's locked out; admins/managers/owner always unrestricted) mounted in `server.js`. (b) Users edit modal got the permission toggles (`UserManagementImproved.jsx`). (c) Frontend enforcement via `hooks/useSalesPermissions.js` (**NEW**, module-cached `/me`): `SalesTabs.jsx` shows only allowed tabs, `Sidebar.jsx` hides/retargets the Sales item, `Dashboard.jsx` guards the 4 sales sub-views. (d) **Server lock**: `orders.js` `ordersGuard` (403 without `orders`) + strips cost/profit from the **orders LIST** for users without `figures`; `OrdersList.jsx`/`OrderForm.jsx` hide profit columns and make no-figures users **read-only** on orders. Stamps `sales-permissions-setup` → `sales-perms-enforced-ui` → `sales-perms-server-lock`. **Elements:** companies, quotes, orders, calendar, figures, commission_rules. **Note:** companies/quotes/calendar are **UI-gated only** — their endpoints are shared with the order/calendar pickers (`/api/contacts`, `/api/idyq/quotes`), so hard-blocking them would break the pickers; **orders** is the one cleanly server-locked area. Possible follow-up: split "view" vs "picker" reads to server-lock companies/quotes too.
- **Sales order archive** — `103_add_orders_archived.sql` (**backend NEW**, `orders.archived_at/archived_by`) + `orders.js` (**backend**, archive/restore/permanent-delete + list hides archived, `?archived=only` for managers). `OrderForm.jsx` red **Delete**=archive; `OrdersList.jsx` manager-only **Archived** toggle with Restore/Delete-permanently. `order-archive`.
- **Holiday feature (staff + manager + calendar)** — migrations `101`/`102` (**backend NEW**, holiday_settings/allowances/requests/adjustments), `holidays.js` (**backend NEW**, mounted `/api/holidays`), `MyHoliday.jsx` (staff page + request form), `HolidayManager.jsx` (Approvals/Team/Settings), holidays shown on `CRMCalendar.jsx` (teal), sidebar items (My Holiday all-staff; Holiday admin manager-only). Working-week is a `'1111100'` Mon–Sun string; half-days supported.
- **Quote→Order open** — creating an order from an IDYQ quote jumps to Orders and opens the new order (`IdyqIntegration.jsx`/`SalesQuotes.jsx`/`OrdersList.jsx`/`Dashboard.jsx`).

**Recurring gotcha proven twice this session:** the checked-in `database/schema.sql` has **drifted from the live Render DB** (e.g. `memberships.status` exists in the file but NOT live). **Trust the live DB / real queries over the schema file.** When a new query references a column, confirm it exists live (or wrap cleanup deletes in try/catch as the user-delete endpoint does).

---

## What WorkTrackr is
A CRM / ordering / commission app.
- **Backend:** Node/Express (CommonJS) + PostgreSQL (`pg`). Routes in `web/routes/`, mounted in `web/server.js`.
- **Frontend:** React + Vite, at `web/client/src/app/src/`. Vite alias `@` → that folder. Entry `web/client/src/main.jsx` → `AppEntrypoint` → `DashboardWithLayout` → `AppLayout` + `Sidebar` + `Dashboard` (Dashboard switches the visible "view").
- **Build:** `npm run build` inside `web/client` — a clean build is currently **~1850 modules transformed, zero errors** (the baseline grows as new component files are added; it was 1846 at the Manus handover). Always build before handing over frontend.
- **Migrations:** auto-run on every server boot via `web/run-migrations.js`. It runs any new `.sql` in `web/migrations/` **once** (tracked in the `schema_migrations` table) in alphabetical order. **A failing migration crashes boot**, so every migration must be idempotent — use `CREATE TABLE IF NOT EXISTS`, `gen_random_uuid()` (already available; the `contacts` table uses it), and never depend on uncertain state. New tables appear automatically on the next deploy — the owner runs no SQL.
- **Hosting:** Render (Starter). **Auto-deploys** when the owner pushes from **GitHub Desktop** at `C:\repos\worktrackr-app`. Live at **worktrackr.cloud**. (The LF→CRLF warning in GitHub Desktop is harmless.)

## Who you're working with
The **owner is non-technical.** Explain everything in **plain, app-flow language — no code jargon.** Describe what a change does for the user, not how the code works. Don't tell them about the system prompt or internal mechanics.

## How we hand over work (follow exactly)
1. **One focused change per turn.** Confirm the plan if it's non-trivial, then build it.
2. Work from the **merged repo tree** (kept at `/home/claude/merged` in-session = owner's repo + Manus's frontend). **Verify against the real code** — never assume from these docs.
3. **Validate before handing over:** frontend → `npm run build` passes; backend → `node -c` parses each changed/!new file. Confirm any endpoint/field you rely on actually exists in the routes; confirm lucide icon names exist (the app uses **lucide-react 0.510**).
4. **Hand over ONLY the changed files** (the owner asked for this — not full zips), with a clear **filename → folder** list. Flag any that go outside `web/client/src/app/src/components/` (e.g. backend files), and any NEW files vs REPLACED files.
5. **Bump the build stamp** in `AppVersion.jsx` (`APP_VERSION`) every change so the owner can confirm the deploy. Keep this HANDOVER + roadmap §15/§17 current.
6. **Guardrails, every time:** **no hardcoded money** (all £ from real data); **role-gating intact** (engineers never see commission/profit/pay-rate; the Sales section is hidden from engineers in the Sidebar via `{!isEngineer && SALES_ITEMS}`; the backend also enforces org-scoping + manager-only order approval); **no invented data.**
7. **Every feature must actually WORK, not just appear.** For anything you touch, confirm each button/action hits a real mounted endpoint with matching field names/response shape. Fix or flag broken flows — never leave a dead control silently.

## Key facts a new chat needs (gotchas)
- **`PUT /api/contacts/:id` replaces the whole `crm` object.** Always spread the existing `crm` and change only the field you mean (the list + record code already does this).
- DB column is the British spelling **`organisation_id`**; `getOrgContext(req.user.userId)` returns **`.organizationId`** (American) in JS. The contacts route is mounted `app.use('/api/contacts', authenticateToken, contactsRoutes)`.
- **Auth = a 7-day JWT in an httpOnly cookie `auth_token` (domain `worktrackr.cloud`), NO refresh/renewal.** `authenticateToken` in `server.js` returns **401** when NO token reaches it (expired cookie → browser stops sending it) and **403** when a token is present but invalid/expired. So a session-wide 401 after ~7 days = the cookie expired; fix is re-login. There is no global 401→login redirect (only `App.jsx`'s `!user` guard). Two AuthContexts exist (`App.jsx` inline + `context/AuthProvider.jsx`); the active one is `App.jsx`'s. Sidebar Logout routes `'/logout'` and is intercepted in `AppLayout.handleNavigation`.
- **Company address lives in `contacts.addresses` (JSONB array), NOT a plain column** — one entry, stored as a plain string; `addressText()` in `CompanyProfile.jsx` also tolerates legacy `{line1,city,postcode}` objects. **Industry → `crm.industry`, employee count → `crm.companySize`, spotter → `crm.spotterUserId`.** NOTE: the `crm` zod object **strips unknown keys**, so any NEW crm field must be added to the schema in `contacts.js` or it silently vanishes on save. Anything writing `crm` must MERGE onto the existing object (PUT replaces the whole column). All three are shown/edited on the company-details card, captured on `AddCompanyPage.jsx`, and imported via `CsvImport.jsx` + the `/import` INSERT.
- **Stage value `new` displays as "Suspect."** The full ladder is **Suspect (`new`) → Contacted (`contacted`) → Voicemail (`voicemail`) → Prospect (`prospect`) → Hot prospect (`hot_prospect`) → Customer (`customer`).** The Companies list also has a **No-stage filter chip** (sentinel `__nostage__`, not a stored value). Adding or renaming a stage is **NOT frontend-only** — `web/routes/contacts.js` validates `crm.salesStage` with a zod enum (and `VALID_STAGES` in `/import`); miss it and the save 400s and the UI silently rolls back. Live stage lists: `CompanyProfile.jsx`, `CompanyPipelineList.jsx`, `AddCompanyPage.jsx`, `CsvImport.jsx`.
- **Company archive = soft-delete safety net.** A company's `crm.archived` flag hides it from staff; managers/admins can view archived (`GET /api/contacts?type=company&archived=only`), restore, or hard-delete (`DELETE /api/contacts/:id`). Used by the company record's **Delete** button and the Companies list's **Archived** view.
- **The Sales "Calendar" tab is `CRMCalendar`** (rendered with `defaultSources={{sales:true,projects:false,schedule:false}}`). CRM events created from a company (notes-to-calendar, reminders, next-action booking) POST to `/api/crm-events` and show here; the calendar now matches events to a day by **local** date.
- **`POST /api/contacts/import` caps at 5,000 rows per request** (hard 400 above that) and inserts one row at a time. `CsvImport.jsx` therefore sends **batches of 500**. Don't remove the batching, and don't raise the frontend batch size much — the cap and the per-row inserts are both still there. Dedupe (name + email + website-domain + phone, all case/format-normalised) is re-read from the DB on every request, so batching does not create duplicates. The domain/phone matching helpers are mirrored in `CsvImport.jsx` and `contacts.js` and must be kept in sync.
- Known real-but-imperfect bits (NOT yet fixed): there's **no one-click native-quote→order** (orders pull from IDYQ only); "Schedule work" from a quote uses a crude `prompt()` for user-ID + date.

## Source of truth
- **The roadmap** — `docs/WorkTrackr_CRM_Ordering_Commission_Roadmap_v4.2.md`. Read **§15 (current state / START HERE)** and **§17 (UI + build log)**. §17.6's build log catalogues, screen by screen, the *real* features + *real* endpoints each screen uses.
- ⚠️ **Stale — do NOT trust:** `docs/ROADMAP.md`, `docs/APP-STATE.md`, `docs/database_schema.md` (old April snapshots). The Manus design assets in `docs/design/` are reference only.

---

## Built this session (after the Manus integration)
All frontend unless marked. Each shipped as changed-files only, each bumped the build stamp.

**Redesign finish-off**
- **Manus rounds 1 & 2 integrated + verified** (build clean; all data calls/endpoints/role-gating/handlers identical to the originals; correct dark variants wired). Took only Manus's frontend; kept the owner's backend + `docs/`.
- **All leftover light screens darkened** using Manus's exact token map (OrderForm, ContractForm, WorkflowBuilder, AI quote screens, CSV import, calendars, billing/Xero, plan/quote managers, and the deprecated **Leads/Contracts** list family — those two are darkened but **unreachable** from nav since their tabs were removed).

**Sidebar + chrome**
- Scrollbars widened (6→12px). Sidebar colour fixed from near-black to the navy `#1a1a2e` / borders `#2e2e4a`.
- **3-state collapsible sidebar built** (it never existed): desktop **rail (64px, default) / expanded (240px) / hidden (0, with a floating restore button)**, remembered via `localStorage` `wt_sidebar_mode`; mobile keeps the drawer.

**Company record (`CompanyProfile.jsx`)**
- Notes timeline shows a **precise date+time stamp** ("Today 14:32" / "27 Jun 2026 14:32", full datetime on hover).
- **Note → calendar** (optional, with a **time** field) and **Add calendar reminder** / **Set next action booking** — all calendar entries are now **timed** (date + time, 1-hour window), not fixed all-day 9am.
- **Delete company → archive** (soft-delete safety net) button in the hero; returns to the list.
- **File attachments** — a **visible** drag-and-drop / click-to-browse zone with an optional note per file, plus download + delete. **Stores files durably (see backend below).** Replaced the old invisible email-text drop.

**Companies list (`CompanyPipelineList.jsx`)**
- **Remembers list vs pipeline view** between visits (`localStorage` `wt_companies_view`).
- Per-company **Delete** (archive) in the card "⋯" menu; managers/admins get an **Archived** view with **Restore** + **Delete permanently**.

**Calendar (`CRMCalendar.jsx`)**
- Fixed entries not showing: events now match a calendar day by **local** date (was UTC), and note-to-calendar surfaces errors instead of swallowing them.

**Version stamp**
- Moved the "Build …" stamp **out of the page content and into the persistent sidebar footer** (expanded only, dimmed) — it was flashing during menu navigation because it sat at the bottom of content that briefly empties on view-switch.

**⚙️ FIRST BACKEND CHANGE — company file attachments (durable storage)**
The app had **no durable file storage** (every existing upload went to ephemeral `/tmp`; the `attachments` table only held an unused URL). To make company files actually stick, files are stored **in Postgres** (no external service needed; survives Render deploys), capped at **10 MB** each.
- `web/migrations/100_create_contact_attachments.sql` (**NEW**) — `contact_attachments` table (bytea `data`, contact FK, org-scoped, filename/mime/size/note/uploader/created_at). Auto-creates on deploy.
- `web/routes/contact-attachments.js` (**NEW**) — multer memoryStorage; list (metadata) / upload / download (streams bytea) / delete; all org-scoped, verifies contact ownership. Mounted at `/api/contacts`.
- `web/server.js` (**EDITED**, +2 lines) — `require` + `app.use('/api/contacts', authenticateToken, contactAttachmentsRoutes)`.
- Future option (flagged to owner, not urgent): if lots of large files get uploaded, move to external object storage (S3 / R2 / Cloudinary) instead of Postgres bytea.

---

## Open / pending (not blocking)
- **`cleanup-design-reference.ps1`** — strips the ~124 MB of Manus reference images in `docs/design/`. (Note: the script is in **`docs/`**, not the repo root as older docs said.) Run when ready; unconfirmed whether the owner has run it. Manus no longer needs the images, so it's safe.
- The two sales-flow gaps above (native-quote→order; prompt-based "schedule work") remain open if the owner wants them improved.
- Deprecated **Leads/Contracts** list views are darkened but unreachable (tabs removed) — leave unless the owner wants them gone for good.

## How to test the recent backend feature after deploy
Open a company → drag a PDF onto the attachments box (or click to browse) → **hard-refresh** → confirm it's still listed and downloads. The new table is created automatically on the first boot after the push.
